import { environment } from './../../../environments/environment';

export const AppConstant = {
    'apiUrl' :  environment.apiVidCord, //Constant apis url
   'googleApi' : '/auth/login-with-google' //Googl Api
}